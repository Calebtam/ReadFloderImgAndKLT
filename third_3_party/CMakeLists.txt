cmake_minimum_required(VERSION 3.3)
project(thirdparty)
find_package(Eigen3 REQUIRED)
find_package(OpenCV 4 QUIET)
if (NOT OpenCV_FOUND)
    find_package(OpenCV 3 REQUIRED)
endif ()
add_subdirectory(CvBridgeSimple)
add_subdirectory(rosbag)
